import React from 'react';
import { Text,Image, Box } from '@chakra-ui/react'
import CartCard from './CartCard';

const Cart = () => {
    return (
        <Box>
            <CartCard/>
        </Box>
    );
};

export default Cart;